version: 2

sources:
  - name: raw
    description: "Datos crudos descargados del SIH CONAGUA"
    schema: raw
    tables:
      - name: presas_diario
        description: "CSV diario por presa, 1 fila por (dam_key, fecha)"
        columns:
          - name: dam_key
            tests: [not_null]
          - name: fecha
            tests: [not_null]